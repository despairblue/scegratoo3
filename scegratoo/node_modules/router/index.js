const router = require('koa-router')()
const cool = require('cool-ascii-faces')
const {
  range
} = require('ramda')

const times = process.env.TIMES || 5

router.get('/', function * () {
  this.body = range(0, times).map(() => cool()).join('\n')
})

export default router
